# Core
torch>=2.1
torchaudio>=2.1
numpy>=1.24

# transformers backend (WavLM / wav2vec2 / HuBERT)
transformers>=4.40

# funasr backend (emotion2vec+) -- pulls in modelscope
funasr>=1.1.0

# audio I/O fallback when a torchaudio backend is unavailable
soundfile>=0.12

# layer-wise probing (train_probes / feature_dataset / probe_metrics):
# stratified split, EER/confusion metrics, and confusion-matrix PNGs
scikit-learn>=1.3
matplotlib>=3.7
